# Copy to .env for local live-mail tests and pass it with:
# python -m payroll_pdf_mailer --env-file .env --send ...

PAYROLL_SMTP_HOST=smtp.example.test
PAYROLL_SMTP_PORT=587
PAYROLL_SMTP_USER=payroll@example.test
PAYROLL_SMTP_PASS=
PAYROLL_SMTP_FROM=payroll@example.test
PAYROLL_SENDER_NAME=Payroll Demo Team
